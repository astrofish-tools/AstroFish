# AstroFish 1.0.0 验证记录

## 正式发布检查

- Release APK 编译成功，目标 Android API 10。
- 应用标签 `AstroFish`，包名 `com.yxy.astrofish`，版本名 `1.0.0`，版本码 `100`。
- 正式包与原 A7R Astro、早期 Live View 测试包的应用身份彼此独立。
- 使用 AstroFish 专用 RSA 2048 位发布密钥签名；APK v1、v2 签名验证通过。
- Release 构建中的 `lintVitalRelease` 通过。
- Sony 接口 stubs 只参与编译，不作为实现打入 APK。

## 自动测试

- ISO 能力列表：10 项边界案例通过。
- 拍摄调度：3027 项断言通过。
- 亮度直方图：12 项断言通过。
- 快门显示格式：10 项断言通过。

## 文档与发布物

- 中英双语 PDF：8 页 A4，逐页渲染检查通过。
- 固定版式 DOCX：8 页，逐页渲染检查通过。
- Markdown 原稿、界面 SVG/PNG、Release Notes、许可证和公开签名证书已包含在发布目录。
- DOCX 和源代码 ZIP 压缩结构检查通过。

## 真机范围

开发过程中的测试版已经在目标使用流程中试用，用户确认当前未发现明显问题。正式版相对于最后测试版的功能代码保持不变，发布阶段只更换应用名称、独立包名、版本号、日志目录和发布签名。

当前正式支持目标仍为第一代 Sony α7R（ILCE-7R）。α7R II、α7S、α7S II 等机型需要分别核对按键扫描码、CameraEx 功能、电子镜头控制、Star View 增强、直方图、回放和 BULB 行为。

桌面测试与签名验证不能替代所有机身、镜头、固件和存储卡组合的实测。

